{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -d apps/frontend ]; then npm install --workspace=apps/frontend --include-workspace-root; else npm install; fi",
  "buildCommand": "if [ -d apps/frontend ]; then npm run build:vercel; else npm run build; fi",
  "outputDirectory": ".next"
}
